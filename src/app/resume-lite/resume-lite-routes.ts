import { Routes, RouterModule } from '@angular/router';
import { ResumeLiteComponent } from "./resume-lite.component";

export const RESUME_LITE_ROUTES: Routes = [
    { path: 'cv', component: ResumeLiteComponent }
];